#include <stdio.h>

int main(int argc, char **argv) {

  
  printf("Server arg count: %d\n", argc);
  printf("mod hry: %s\n", argv[1]);
  printf("typ sveta: %s\n", argv[2]);
  printf("cas hry: %s\n", argv[3]);
  printf("sirka pola: %s\n", argv[4]);
  printf("vyska pola: %s\n", argv[5]);
  printf("nazov suboru: %s\n", argv[6]);

  return 0;
}
